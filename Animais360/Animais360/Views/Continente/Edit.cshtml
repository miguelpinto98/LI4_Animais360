@model Animais360.Models.Continente

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Continente</legend>

        @Html.HiddenFor(model => model.ContinenteId)

        <div class="editor-label">
            @Html.LabelFor(model => model.ContinenteName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ContinenteName)
            @Html.ValidationMessageFor(model => model.ContinenteName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Descricao)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Descricao)
            @Html.ValidationMessageFor(model => model.Descricao)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
